import NavHeader from '../components/NavHeader';

const TensionAtDepth = () => {
  // input: depth, percentage pull out
  // output: conservative pullout, aggressive pullout
  return (
    <>
      <NavHeader>Max. Pull Out at Depth</NavHeader>
    </>
  );
};

export default TensionAtDepth;
