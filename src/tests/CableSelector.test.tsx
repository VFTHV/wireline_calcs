import { CableSelector } from '../components';
import { render, screen } from '@testing-library/react';

// it('renders cable selector', () => {
//   render(<CableSelector />)
//   const selectElement = screen.
//   expect(selectElement).
// })
